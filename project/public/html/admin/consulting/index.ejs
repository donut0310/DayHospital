<%- include('../header') %>
    <ul class="list-group">
        <% preposts.map((prepostlist) => { %>
            <li class="list-group-item" onclick="location.href=('/admin/prepostlist/<%=prepostlist.ID%>');"><%= prepostlist.TITLE %></li></a>
        <% }) %>
    </ul>

    <nav>
        <ul class="pagination">
            <li>
                <a href="#" aria-label="Previous">
                    <span aria-hidden="true">&laquo;</span>
                  </a>
            </li>
            <% for (let i =1; i<=parseInt(preposts.length)/10 +1; i++) { %>
                <li><a href="?page=<%=i%>"><%= i %></a></li>
            <% } %>
                <li>
                    <a href="#" aria-label="Next">
                        <span aria-hidden="true">&raquo;</span>
                    </a>
                </li>
        </ul>
        <a href ="./prepostlist/register"><button type="button" value="글쓰기" style="float: right; margin-right: 20px;">글쓰기</button></a>
    </nav>

<%- include('../footer') %>

