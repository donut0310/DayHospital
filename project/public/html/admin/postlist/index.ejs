<%- include('../header') %>
    <ul class="list-group">
        <% posts.map((postlist) => { %>
            <li class="list-group-item" onclick="location.href=('/admin/postlist/<%=postlist.ID%>');"><%= postlist.TITLE %></li></a>
        <% }) %>
    </ul>

    <nav>
        <ul class="pagination">
            <li>
                <a href="#" aria-label="Previous">
                    <span aria-hidden="true">&laquo;</span>
                  </a>
            </li>
            <% for (let i =1; i<=parseInt(posts.length)/10 +1; i++) { %>
                <li><a href="?page=<%=i%>"><%= i %></a></li>
            <% } %>
                <li>
                    <a href="#" aria-label="Next">
                        <span aria-hidden="true">&raquo;</span>
                    </a>
                </li>
        </ul>
        <a href ="./postlist/register"><button type="button" value="글쓰기" style="float: right; margin-right: 20px;">글쓰기</button></a>
    </nav>

<%- include('../footer') %>

